cc.Class({
    extends: cc.Component,

    properties: {
        scienceImgs: {
            default: [],
            type: cc.SpriteFrame,
        }
    },

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {},

    // start () {},

    // update (dt) {},
});
